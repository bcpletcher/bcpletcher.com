rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;  // Always allow read
      allow write: if false; // Keep write restricted
    }
  }
}
